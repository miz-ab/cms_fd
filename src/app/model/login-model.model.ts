export interface LoginModel {
    username: string
    password: string
}

export interface user {
    id: string
    password: string
    name: string
    email:string
    role: string
    gender:string
}
